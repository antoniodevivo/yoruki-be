import { Users } from "../entities/users.entity"

export default {
    entity: Users,
    items: {
        admin: {
            fullName: "admin",
            email: "admin@yoruki.com"
        }
    }
}